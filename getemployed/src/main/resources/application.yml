spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/getemployed
    username: getemployed
    password: getemployed

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml

server:
  port: 8080


