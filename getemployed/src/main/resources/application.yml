spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/career_trajectory
    username: career
    password: career

  jpa:
    hibernate:
      ddl-auto: none

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.yaml


